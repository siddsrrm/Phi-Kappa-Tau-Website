{
  "name": "phi-kappa-tau-website-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix client run dev",
    "start": "npm --prefix client run start",
    "build": "npm --prefix client run build",
    "clean": "echo \"No clean script configured for Next.js\""
  }
}
